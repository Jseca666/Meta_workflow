# Pro upload package

Upload or paste only the files listed in `pro_material_manifest.md`.

Keep local-only values out of public evidence:

- browser profile id
- send nonce
- expected conversation id
- browser target id
- raw cookies, tokens, or private browser state

When a fixed GitHub source ref is available, prefer linking that ref in the prompt instead of uploading source snapshots.
