# Output lifecycle

Public outputs:

- protocols
- templates
- example configs
- source-grounded prompts
- sanitized validation summaries

Runtime outputs:

- browser locks
- project session status
- monitor status
- raw Pro responses
- local ledgers
- local configs

Runtime outputs stay under `integrations/pro_bridge/runtime/` or `*.local.*` files and are ignored by git.
